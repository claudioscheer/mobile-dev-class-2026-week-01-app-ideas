name: ci

on:
  push:
    branches: [main]
  pull_request:

jobs:
  check:
    runs-on: ubuntu-latest
    container: node:20-alpine
    steps:
      - uses: actions/checkout@v4
      # No npm install: public-checks.mjs uses only Node built-ins
      - run: node scripts/public-checks.mjs
